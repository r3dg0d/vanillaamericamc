const state={csrf:"",user:null,consoleTimer:null};
const $=(selector,root=document)=>root.querySelector(selector);
const $$=(selector,root=document)=>[...root.querySelectorAll(selector)];
async function api(path,options={}){const headers={"Accept":"application/json",...(options.headers||{})};if(options.body){headers["Content-Type"]="application/json";headers["X-CSRF-Token"]=state.csrf}const response=await fetch(path,{credentials:"same-origin",...options,headers});if(response.status===204)return null;const data=await response.json().catch(()=>({error:"Invalid server response"}));if(!response.ok)throw new Error(data.error||"Request failed");return data}
async function initialize(){try{const session=await api("/api/session");state.csrf=session.csrf}catch{}refreshStatus();loadCatalog();setInterval(refreshStatus,30000);try{state.user=await api("/api/me");showStaff()}catch{}}
async function refreshStatus(){const chip=$("#hero-status"),panel=$("#status-panel");try{const status=await api("/api/status");panel.setAttribute("aria-busy","false");$("#status-state").textContent=status.online?"Online":"Offline";$("#status-players").textContent=status.online?`${status.players} / ${status.max_players}`:"—";$("#status-version").textContent=status.version||"—";$("#status-latency").textContent=status.online?`${status.latency_ms} ms`:"—";chip.querySelector("span:last-child").textContent=status.online?`${status.players} players online`:"Server offline or awaiting EULA";chip.querySelector(".status-dot").className=`status-dot ${status.online?"online":"offline"}`}catch{$("#status-state").textContent="Unavailable";chip.querySelector("span:last-child").textContent="Status service unavailable"}}
async function loadCatalog(){const target=$("#catalog");try{const data=await api("/api/catalog");target.innerHTML=data.items.map(item=>`<article class="catalog-item"><p class="eyebrow">${escapeHTML(item.category)}</p><h3>${escapeHTML(item.name)}</h3><p>${escapeHTML(item.description)}</p><p class="price">Test value: $${(item.price_cents/100).toFixed(2)}</p><label>Quantity <input type="number" min="0" max="10" value="0" data-item="${escapeHTML(item.code)}"></label></article>`).join("")}catch{target.innerHTML="<p>Catalog unavailable. No order can be recorded.</p>"}}
function showStaff(){if(!state.user)return;$("#staff-panel").hidden=false;$("#staff-identity").textContent=`${state.user.username} · ${state.user.role}`;$("#login-dialog").close();$$(".admin-only").forEach(element=>element.hidden=state.user.role!=="administrator");loadReports();if(state.user.role==="administrator"){loadAudit();refreshConsole();state.consoleTimer=setInterval(refreshConsole,3500)}location.hash="staff-panel"}
async function loadReports(){const target=$("#reports-list");try{const reports=await api("/api/staff/reports");target.innerHTML=reports.length?reports.map(report=>`<article class="report-row"><strong>${escapeHTML(report.target_name)} · ${escapeHTML(report.status)}</strong><p>${escapeHTML(report.reason)}</p><small>${escapeHTML(report.id)} · reported by ${escapeHTML(report.reporter_name)}</small>${report.status==="open"?`<div class="button-row"><button data-report="${escapeHTML(report.id)}" data-action="resolve">Resolve</button><button data-report="${escapeHTML(report.id)}" data-action="dismiss">Dismiss</button></div>`:""}</article>`).join(""):"<p>No reports recorded.</p>";$$("[data-report]",target).forEach(button=>button.addEventListener("click",()=>reportAction(button.dataset.report,button.dataset.action)))}catch(error){target.textContent=error.message}}
async function reportAction(id,action){const note=prompt(`${action} note (optional, max 240 characters)`)??"";try{await api(`/api/staff/reports/${encodeURIComponent(id)}`,{method:"POST",body:JSON.stringify({action,note})});loadReports()}catch(error){alert(error.message)}}
async function loadAudit(){try{const entries=await api("/api/admin/audit");$("#audit-list").innerHTML=entries.map(entry=>`<article class="audit-row"><strong>${escapeHTML(entry.action)}</strong><p>${escapeHTML(entry.actor)} → ${escapeHTML(entry.target)} · ${escapeHTML(entry.outcome)}</p><small>${escapeHTML(entry.created_at)}</small></article>`).join("")||"<p>No staff actions yet.</p>"}catch(error){$("#audit-list").textContent=error.message}}
async function refreshConsole(){try{const data=await api("/api/admin/console?lines=180");const output=$("#console-output");output.textContent=data.available?data.lines.join("\n"):"Console unavailable until the server has started.";output.scrollTop=output.scrollHeight}catch{}}
function escapeHTML(value){return String(value??"").replace(/[&<>"']/g,character=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[character]))}
$("#staff-login").addEventListener("click",()=>$("#login-dialog").showModal());$("#close-login").addEventListener("click",()=>$("#login-dialog").close());$(".menu-button").addEventListener("click",event=>{const nav=$("#site-nav"),open=nav.classList.toggle("open");event.currentTarget.setAttribute("aria-expanded",String(open))});$$("nav a").forEach(link=>link.addEventListener("click",()=>$("#site-nav").classList.remove("open")));$$(".copy-button").forEach(button=>button.addEventListener("click",async()=>{try{await navigator.clipboard.writeText(button.dataset.copy);button.textContent="Copied"}catch{button.textContent="Copy unavailable"}setTimeout(()=>button.textContent="Copy address",1600)}));
$("#login-form").addEventListener("submit",async event=>{event.preventDefault();const data=Object.fromEntries(new FormData(event.currentTarget));try{const result=await api("/api/auth/login",{method:"POST",body:JSON.stringify(data)});state.csrf=result.csrf;delete result.csrf;state.user=result;0 0"#login-result").textContent="Signed in.";showStaff()}catch(error){$("#login-result").textContent=error.message}});
$("#logout").addEventListener("click",async()=>{try{await api("/api/auth/logout",{method:"POST",body:"{}"})}catch{}state.user=null;clearInterval(state.consoleTimer);$("#staff-panel").hidden=true;location.hash="home"});
$("#checkout-form").addEventListener("submit",async event=>{event.preventDefault();const items={};$$("[data-item]").forEach(input=>{const quantity=Number(input.value);if(quantity>0)items[input.dataset.item]=quantity});try{const order=await api("/api/orders",{method:"POST",body:JSON.stringify({player_uuid:$("#player-uuid").value,items})});$("#order-result").textContent=`${order.message} Order ${order.id}; test total $${(order.total_cents/100).toFixed(2)}.`}catch(error){$("#order-result").textContent=error.message}});
$("#moderation-form").addEventListener("submit",async event=>{event.preventDefault();const data=Object.fromEntries(new FormData(event.currentTarget));data.duration_seconds=Number(data.duration_seconds);try{const result=await api("/api/staff/moderation",{method:"POST",body:JSON.stringify(data)});$("#moderation-result").textContent=`Action queued as ${result.id}.`;loadAudit()}catch(error){$("#moderation-result").textContent=error.message}});
$$("[data-server]").forEach(button=>button.addEventListener("click",async()=>{if(button.dataset.server==="stop"&&!confirm("Gracefully stop the VA+ server?"))return;try{await api("/api/admin/server",{method:"POST",body:JSON.stringify({action:button.dataset.server})});button.textContent="Requested";setTimeout(()=>button.textContent=button.dataset.server[0].toUpperCase()+button.dataset.server.slice(1),1500);loadAudit()}catch(error){alert(error.message)}}));
$("#console-form").addEventListener("submit",async event=>{event.preventDefault();const data=Object.fromEntries(new FormData(event.currentTarget));try{const result=await api("/api/admin/console",{method:"POST",body:JSON.stringify(data)});event.currentTarget.reset();if(result.output)alert(result.output);refreshConsole();loadAudit()}catch(error){alert(error.message)}});
$("#user-form").addEventListener("submit",async event=>{event.preventDefault();const data=Object.fromEntries(new FormData(event.currentTarget));try{await api("/api/admin/users",{method:"POST",body:JSON.stringify(data)});$("#user-result").textContent="Account created.";event.currentTarget.reset();loadAudit()}catch(error){$("#user-result").textContent=error.message}});
initialize();

